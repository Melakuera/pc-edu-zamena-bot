application:
  name: ${BOT_NAME}
  version: 0.1

telegram:
  api-url: https://api.telegram.org/
  bot-name: ${BOT_NAME}
  bot-token: ${BOT_TOKEN}
  webhook-path: ${WEBHOOK_PATH}
  example-gif-file-id: ${EXAMPLE_GIF_FILE_ID}
  bot-username: ${BOT_USERNAME}

# deprecated
cron:
  expression: ${CRON_VALUE}

server:
  port: ${SERVER_PORT}

spring:
  data:
    mongodb:
      uri: ${MONGODB_URI}